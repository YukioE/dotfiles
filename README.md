# dotfiles
arch + swaywm dotfiles

Run `./init.sh` to bootstrap the system. Pass any argument to skip the system
upgrade and package installation during bootstrap, for example
`./init.sh skip-upgrade`.
